{
  "name": "e2e",
  "version": "1.0.0",
  "description": "e2e",
  "private": "true",
  "license": "MIT",
  "dependencies": {
    "@playwright/test": "^1.22.2",
    "@fakehost/utils-app-server": "^0.0.18",
    "chalk": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "build": "rimraf dist && tsc ",
    "test": "npx playwright test -c src/config/config.ts",
    "-pretest": "npx playwright@1.22.2 install"
  }
}
